name: amap_kit
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  json_serializable: ^4.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.5

flutter:
  plugin:
    platforms:
      android:
        package: com.yuro.amap_kit
        pluginClass: AmapKitPlugin
      ios:
        pluginClass: AmapKitPlugin
